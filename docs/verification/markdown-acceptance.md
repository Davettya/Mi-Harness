# 模型 Markdown 渲染验收

2026-09-18，本次仅调整 Web 展示层和 npm 锁定依赖，未修改后端或持久化消息格式。

- `npm --prefix apps/web test`：27 项通过（6 个测试文件）。新增 5 项覆盖常用语法、GFM 表格/任务清单/删除线、原始 HTML 不执行、危险协议/本地相对 URL 拦截、外链及图片链接、流式未闭合代码围栏。
- `npm --prefix apps/web run build`：TypeScript 和 Vite 生产构建通过。
- 浏览器在独立临时实例中提交 Markdown，由确定性 Demo 回显为 assistant 消息，验证标题、强调、列表、引用、表格、代码块和外链。用户原始输入保留纯文本格式。
- 已完成消息与流式累积文本共用同一 `MarkdownText` 组件；不变的历史消息通过 memo 避免重复解析。
- 图片只显示可点击链接；原始 HTML 作为文本处理；代码语法高亮、数学公式与 Mermaid 不在本次范围内。
- 前端构建由运行中的服务直接提供，刷新页面即可加载，不需要迁移数据或重启后端。
